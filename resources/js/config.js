let APP_URL='http://pure-plugin'

let APP_API='http://127.0.0.1:8000/api'

let configColor=document.getElementById('debtorsTableContainer').dataset.color

let configHeight=document.getElementById('debtorsTableContainer').dataset.height

let configScrollable=document.getElementById('debtorsTableContainer').dataset.scrollable

export {APP_URL,APP_API,configHeight,configColor,configScrollable}